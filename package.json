{
  "name": "builder-setup",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run src/setup.ts",
    "build": "bun run scripts/build.ts",
    "lint": "bun scripts/oxlint.ts src/ scripts/",
    "format": "bun scripts/oxlint.ts --fix src/ scripts/",
    "check": "bun run lint && bun --bun tsc"
  },
  "devDependencies": {
    "bun-types": "^1.3.11",
    "lefthook": "^2.1.5",
    "oxlint": "^1.59.0",
    "typescript": "^6.0.2"
  }
}
