pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx oxfmt {staged_files}
      stage_fixed: true
    typecheck:
      run: bun --bun tsc
