pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: oxlint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*.{ts,tsx,js,jsx}"
      run: oxfmt {staged_files}
      stage_fixed: true
    typecheck:
      run: bun --bun tsc
